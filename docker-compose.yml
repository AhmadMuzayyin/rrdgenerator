services:
  rrdgenerator:
    image: barokah-rrdgenerator:latest
    build: .
    ports:
      - "7101:3000"
    restart: unless-stopped
    networks:
      - barokah-network

networks:
  barokah-network:
    name: barokah-network
    external: true
