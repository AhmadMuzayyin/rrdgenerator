services:
  rrdgenerator:
    image: barokah-rrdgenerator:latest
    build: .
    ports:
      - "7101:3000"
    restart: unless-stopped
    networks:
      - rrdgenerator-network

networks:
  rrdgenerator-network:
    name: rrdgenerator-network
